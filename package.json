{
  "name": "general-set",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && yarn build",
    "server": "cd server && yarn start",
    "start": "concurrently --kill-others-on-fail \"yarn server\" \"yarn build\""
  },
  "dependencies": {}
}
